{% load item_extras %}

<h1>{{ store.storeName }}</h1>

<!--  <p>{{ store.pk}}</p>-->
<img src="{{ store.storeAvatar.url }}" width="300"/></br>

<ul>
{% for category in categories %}
	<li>
		<h2>{{ category.categoryName }}</h2>
		{% for item in items|items_in_category:category %}
			<p>{{item.name}}</p>
			<p>{{item.description}}</p>
		{% endfor %}
	</li>
{% endfor %}
</ul>